public class Square extends Rectangle {
	
	static double _length;
	static double _width;
	
	public Square (double length, double width){
		super(length, width);
		_length = length;
		_width = width;
		
		//important to note/possible code that the length and width should be the same
		
		
	}
}
